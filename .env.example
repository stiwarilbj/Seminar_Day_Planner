# The demo password works on localhost. Change it before using LAN mode.
STAFF_PASSWORD=seminar-day
SESSION_SECRET=change-this-before-lan-use
HOST=127.0.0.1
PORT=8080
ALLOWED_EMAIL_DOMAIN=
DATABASE_PATH=runtime/database/seminar_day.db
